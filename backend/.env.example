# ─────────────────────────────────────────────────────────
# MBM Canteen Hub — Backend Environment Variables Example
# ─────────────────────────────────────────────────────────
# Copy this file to .env and replace the values with your actual configuration.
# In Lambda: set these directly in the Lambda console under Configuration → Environment Variables.

# Network Port Configuration (used for local dev only — Lambda ignores PORT)
PORT=5000

# Node Environment Mode (development | production)
NODE_ENV=development

# MongoDB Atlas Connection String
# Example: mongodb+srv://<username>:<password>@cluster.mongodb.net/dbname
# In Atlas: Network Access → 0.0.0.0/0 (Lambda IPs are dynamic)
MONGO_URI=mongodb+srv://your_db_user:your_db_password@your_cluster.mongodb.net/mbm_canteen

# JWT Secret for Session Authentication
# Tip: Generate a secure 64-character random string
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRE=7d

# CORS Allowed Origin for Frontend Client
# In Development:  http://localhost:5173
# In Production:   https://YOUR_CLOUDFRONT_ID.cloudfront.net
FRONTEND_URL=http://localhost:5173
FRONTEND_URL_WWW=http://localhost:3000
